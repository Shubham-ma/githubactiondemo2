name: ETL Pipeline Demo


on:
   push:
      branches:
      - main


jobs:
   extract-job:
        runs-on: ubuntu-latest
        steps:
          - name: checkout code
            uses: actions/checkout@v2



          - name: Run extract.py
            run: python exract.py

   transform-job:
       runs-on: ubuntu-latest
       needs: extract-job
       steps:
          - name: Checkout code
            uses: actions/checkout@v2

          - name: Run transform.py
            run: python transform.py

   load-job:
       runs-on: ubuntu-latest
       needs: transform-job
       steps:
         - name: checkout code
           uses: actions/checkout@v2

         - name: Run load.py
           run: python load.py

